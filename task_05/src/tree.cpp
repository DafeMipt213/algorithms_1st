#include "tree.hpp"

/*Tree::Tree() {}

bool Tree::Insert(int key, int value) { 
        if (root == nullptr)
        {
            root = new node(add_value);
            return;
        }
        if (add_value >= root->value)
        {
            if (!root->right)
            {
                root->right = new node(add_value);
            }
            else
                AddElement(root->right, add_value);
        }
        else
        {
            if (!root->left)
            {
                 root->left = new node(add_value);
            }
            else
                AddElement(root->left, add_value);
        }
    }

    
    
    
    return true; }

void Tree::InsertOrUpdate(int key, int value) {}

int Tree::Find(int key) const { return 0; }
*/